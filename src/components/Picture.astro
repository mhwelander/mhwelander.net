---
import { getImage } from "astro:assets";
import type { ImageMetadata } from "astro";

interface Props {
    mobileImgUrl: string | ImageMetadata;
    desktopImgUrl: string | ImageMetadata;
    alt: string;
    caption: string;
}

const { mobileImgUrl, desktopImgUrl, alt, caption } = Astro.props;